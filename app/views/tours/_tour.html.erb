<%= link_to tour do %>
<div class="col-md-6">
  <li id="tours-<%= tour.id %>">
    <div class="image">
      <img class="image-home" src="<%= image_home(tour) %>">
    </div>
    <span class="title">
      <%= link_to tour.title, tour %>
    </span>
    <span class="rate">
      <% if tour.avg_rate <= 1 %>
        <%= t(".norate")%>
      <% else %>
        <% for i in 1..(tour.avg_rate)%>
          <span class="fa fa-star checked"></span>
        <% end %>
      <% end %>
    </span>
    <span class="postedby">
       <%= t(".postedby") %><%= tour.account.email %>.
    </span>
  </li>
</div>
<% end %>
